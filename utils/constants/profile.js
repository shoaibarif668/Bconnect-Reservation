

export const ORGANIZER_EVENTS_TAB = Object.freeze({
    CURRENT_EVENTS : "currentEvents",
    PAST_EVENTS : "pastEvents"
})

export const FOLLOWER_TYPE = Object.freeze({
    FOR_SEEKER : 1,
    FOR_ORGANIZER : 2,
})